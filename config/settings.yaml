# Global system configuration

app:
  name: dlnk_v5
  environment: development
  timezone: UTC

api:
  host: 0.0.0.0
  port: 8000

redis:
  url: redis://localhost:6379/0

rabbitmq:
  url: amqp://guest:guest@localhost:5672//

postgresql:
  dsn: postgresql+asyncpg://dlnk:password@localhost:5432/dlnk_v5

logging:
  level: INFO
  structured: true
